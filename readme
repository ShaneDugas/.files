#To sync dotfiles from git

##Install Stow
-apt install stow

mkdir ~/dotfiles
cd dotfiles


git clone https://github.com/shanedugas/.files.git

#This will add links to all files in the parent directory to the current directory
stow .



##On Rocky 9.5
#I had to install epel to find the stow install
#
#	```
#	dnf install epel-release
#	dnf update
#	dnf install stow
#	```
