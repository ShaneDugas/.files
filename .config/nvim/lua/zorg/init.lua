--require ("zorg.remap")
print ("Remaps from the /nvim/lua/zorg/init dot lua")

----
--local function cat_file(motd.lua)
--    local file = io.open(motd.lua, "r") -- Open the file in read mode ("r")
--    if file then
--        local content = file:read("*a") -- Read the entire content of the file
--        file:close() -- Close the file handle
--        if content then
--            print(content) -- Print the content to the console
--        else
--            print("Error: Could not read content from file '" .. filepath .. "'")
--        end
--    else
--        print("Error: Could not open file '" .. filepath .. "'")
--    end
--end
