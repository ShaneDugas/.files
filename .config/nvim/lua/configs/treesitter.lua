-- Configuration for nvim-treesitter
require('nvim-treesitter.configs').setup({
  -- Add your Treesitter configurations here
  -- For example:
  -- ensure_installed = { "c", "lua", "vim", "vimdoc" },
  -- highlight = {
  --   enable = true,
  -- },
  -- indent = {
  --   enable = true,
  -- },
})
